<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fujimiya.farmartrevisi">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SignInActivity"
            android:label="Login to your Account">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PilihActivity"
            android:label="Pilih Status" />
        <activity
            android:name=".SignUpUserActivity"
            android:label="Create New Account" />
        <activity
            android:name=".SignUpCustomerActivity"
            android:label="Create New Account" />
        <activity
            android:name=".AdminActivity"
            android:label="Admin"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DetailPendaftarActivity"
            android:label="Detail Pendaftar" />
        <activity
            android:name=".UserActivity"
            android:label="User"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CustomerDrawer"
            android:label="Customer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PublishActivity"
            android:label="Publis Komoditi" />
        <activity android:name=".ProfilUserActivity" />
        <activity android:name=".DetailPublishActivity" />
        <activity android:name=".DetailListPenjualanActivity" />
        <activity android:name=".SettingAkunActivity" />
        <activity android:name=".SettingAkunUserMainActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".ChatActivity" />
        <activity
            android:name=".DetailPemesananActivity"
            android:label="Detail Pesanan" />
        <activity android:name=".DetailPesananTerimaActivity" />
        <activity android:name=".DetailKonfirmasiPesananActivity"></activity>
    </application>

</manifest>